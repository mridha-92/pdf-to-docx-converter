"""Placeholder for the conversion engine.

Implemented in Step 2: PDF analysis, native conversion via pdf2docx,
span-level fallback formatting via PyMuPDF, and graceful error handling.
"""

from pathlib import Path


def convert_pdf_to_docx(pdf_path: Path, docx_path: Path) -> None:
    raise NotImplementedError("Conversion engine arrives in Step 2.")
